/**
 *
 * Routing => [Pages and Layouts]
 *
 *
 * 1. Pages Router에서는 한 폴더 아래에서 여러 페이지를 라우팅할 수 있었지만,
 *    App Router에서는 라우트 이름 폴더 아래에서 page.tsx를 정의하지 않으면 해당 페이지는 라우팅되지 않는다.
 *
 * */
export default function GoodsPage() {
  return <h1>Hello, Goods Page!</h1>
}
